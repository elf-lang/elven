let Vector = {
	__new = fun(x,y){
		this.x=x
		this.y=y
	}
}

let v = new Vector(1,2)
elf.pf(v)

things = {a=1,b=2,c=3,d=4}
elf.pf(things)
#eof

elf.load_file("monster.elf")
#eof

elf.include("elf.kit")
.create("launch",512,512)
while .step() ? { }
#eof

elf.change_work_dir("W:\\MicroRJ\\ninja-world")
elf.load_file("main.elf")
load "gui.elf"
#eof


a = 1 && 1
a = 1 && 0
elf.pf(a)

#eof

let b = {}
b.a = 777
b.a = 1 && 0
elf.pf(b)
#eof

let b = fun(a,b,c,d) ? {
	elf.pf(a,b,c,d)
}
b(1,2,3,4)


let dummy = {1,2,3,4}
elf.pf(dummy)
dummy.name="Roy, Roy Jacobs"
elf.pf(dummy)

dummy[0]="Chicken"
elf.pf(dummy[0])

this_is_a_global = 777
elf.pf(this_is_a_global)

let things = {1,2,3,4}
elf.pf(things)
things = {a=1,b=2,c=3,d=4}
elf.pf(things)

{
	let a = 1
	let b = 1
	let c = 1
	let d = 1
}
{
	let a = 1
	let b = 1
	let c = 1
	let d = 1
}