InitWindow("Random", 512, 512, 1)
points := {}
for i = 0...100 ? {
	points:add({ x = elf.random() * 512, y = elf.random() * 512 })
}
while PollWindow() ? {
	Clear()
	SetColor(255, 255, 255, 255)
	DrawCircle(64, 64, 8, 2)
	for p = points[...] ? {
		DrawCircle(p.x, p.y, 4, 2)
	}
}